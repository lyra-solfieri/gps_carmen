name: sensors
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  geocoding: ^2.0.2
  geolocator: ^8.1.1
  location: ^4.3.0
  flutter_map: ^2.2.0 #responsible for rendering the map on the screen
  latlong2: ^0.8.1 #gets any lat lng requested


  cupertino_icons: ^1.0.2
  syncfusion_flutter_maps: ^20.2.45
  flutter_map_marker_popup: ^3.2.0
  video_player: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter




flutter:


  uses-material-design: true

